import Reader from './Reader'
import TokenWithData from './TokenWithData'
import Node from './Node'

const parseNode = (
  { 
    getCurrent,
    next
  }: Reader<TokenWithData>
): Node => {

}

export default parseNode
